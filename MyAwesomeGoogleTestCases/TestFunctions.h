#pragma once

//Returns the size of the passed literal in bytes.
int Sizeof(int);
int Sizeof(float);
int Sizeof(double);
int Sizeof(char);

int Count(int); //Counts digits in the integer.
int Fact(int); // Calculates factorial of the number.
double Half(int); //Halves the number.
int Cube(int); //Cubes the number.